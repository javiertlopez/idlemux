language: go

go:
  - 1.15.x

before_install:
  - go get github.com/javiertlopez/awesome/api

script:
  - go test -v ./api -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)